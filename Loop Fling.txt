local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")

-- Delete previous UI
if CoreGui:FindFirstChild("FlingSystem") then
    CoreGui.FlingSystem:Destroy()
end

-- Force UI creation
local GUI = Instance.new("ScreenGui")
GUI.Name = "FlingSystem"
GUI.ResetOnSpawn = false
GUI.ZIndexBehavior = Enum.ZIndexBehavior.Global
GUI.DisplayOrder = 9999
GUI.Parent = CoreGui

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 300, 0, 180)
Main.Position = UDim2.new(0.5, -150, 0.5, -90)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.Active = true
Main.Visible = true -- Explicit visibility
Main.Parent = GUI

-- Visibility enforcement
task.spawn(function()
    while task.wait(1) do
        if not GUI:IsDescendantOf(game) then
            GUI.Parent = CoreGui
            GUI.Enabled = true
            Main.Visible = true
        end
    end
end)

-- Rest of your working code from previous version --
-- [PASTE THE WORKING FLING/CAMERA CODE HERE] --

print("UI initialized at:", Main.AbsolutePosition)
warn("GUI parent:", GUI.Parent)
