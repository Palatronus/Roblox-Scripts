local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- GUI Setup
local gui = Instance.new("ScreenGui")
gui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 120)
frame.Position = UDim2.new(0.5, -100, 0.5, -60)
frame.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
frame.Active = true

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 20)
title.Text = "Fling Controller (Drag)"
title.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
title.TextColor3 = Color3.new(1, 1, 1)
title.Parent = frame

local inputBox = Instance.new("TextBox")
inputBox.Size = UDim2.new(0.9, 0, 0, 25)
inputBox.Position = UDim2.new(0.05, 0, 0.25, 0)
inputBox.PlaceholderText = "Target Username"
inputBox.Parent = frame

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0.9, 0, 0, 25)
toggleBtn.Position = UDim2.new(0.05, 0, 0.55, 0)
toggleBtn.Text = "Start Fling"
toggleBtn.Parent = frame

frame.Parent = gui
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Draggable GUI (same as previous implementations)
-- [Include identical drag code from previous versions]

-- Original Fling Core
local flingActive = false
local connections = {}
local anchor, originalCFrame, originalVelocity, originalRotVelocity

local function align(Part0, Part1)
    -- Keep original alignment function unchanged
    local att1 = Instance.new("Attachment")
    att1.Position = Vector3.new(0, 0, 0)
    local att0 = att1:Clone()

    local ap = Instance.new("AlignPosition", att0)
    ap.ApplyAtCenterOfMass = true
    ap.MaxForce = 9e9
    ap.MaxVelocity = 9e9
    ap.ReactionForceEnabled = false
    ap.Responsiveness = 200
    ap.RigidityEnabled = false
    ap.Attachment1 = att1
    ap.Attachment0 = att0

    att1.Parent = Part1
    att0.Parent = Part0
end

local function returnToOrigin()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        hrp.CFrame = originalCFrame
        hrp.Velocity = originalVelocity
        hrp.RotVelocity = originalRotVelocity
    end
end

local function startFling(targetChar)
    if not targetChar or not targetChar:FindFirstChild("HumanoidRootPart") then return end
    local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    -- Store original state
    originalCFrame = hrp.CFrame
    originalVelocity = hrp.Velocity
    originalRotVelocity = hrp.RotVelocity

    -- Original teleport
    hrp.CFrame = targetChar.HumanoidRootPart.CFrame

    -- Original fling setup
    local parts = {}
    for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
        if part:IsA("BasePart") then
            if part.CanCollide then
                table.insert(parts, part)
            else
                part.Velocity = Vector3.new(0, -1000, 0)
            end
        end
    end

    anchor = Instance.new("Part")
    anchor.Name = "FlingAnchor"
    anchor.Anchored = true
    anchor.Transparency = 1
    anchor.CanCollide = false
    anchor.Size = Vector3.new(2, 2, 1)
    anchor.CFrame = hrp.CFrame
    anchor.Parent = LocalPlayer.Character

    for _, part in ipairs(parts) do
        align(part, anchor)
    end

    -- Original tracking system
    connections.follow = RunService.Stepped:Connect(function()
        if targetChar:FindFirstChild("HumanoidRootPart") then
            anchor.CFrame = targetChar.HumanoidRootPart.CFrame
        end
    end)

    connections.spin = RunService.Heartbeat:Connect(function()
        for _, part in ipairs(parts) do
            part.Velocity = Vector3.new(0, -25.05, 0)
            part.RotVelocity = Vector3.new(9999, 9999, 9999)
        end
    end)
end

local function stopFling()
    flingActive = false
    for _, conn in pairs(connections) do conn:Disconnect() end
    if anchor then anchor:Destroy() end
    returnToOrigin()
end

-- Permanent Tracking System
local function monitorTarget()
    local targetName = inputBox.Text
    local targetPlayer = Players:FindFirstChild(targetName)
    if not targetPlayer then return end

    -- Initial fling
    if targetPlayer.Character then
        startFling(targetPlayer.Character)
    end

    -- Target respawn handler
    connections.respawn = targetPlayer.CharacterAdded:Connect(function(newChar)
        startFling(newChar)
    end)

    -- Local respawn handler
    connections.localRespawn = LocalPlayer.CharacterAdded:Connect(function()
        task.wait(1)
        if targetPlayer.Character then
            startFling(targetPlayer.Character)
        end
    end)
end

-- Toggle Control
toggleBtn.MouseButton1Click:Connect(function()
    flingActive = not flingActive
    toggleBtn.Text = flingActive and "Stop Fling" or "Start Fling"

    if flingActive then
        monitorTarget()
    else
        stopFling()
    end
end)
