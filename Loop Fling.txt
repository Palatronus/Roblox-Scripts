-- Enhanced Fling System (Nameless Admin Style)
local Flinging = false
local function FlingTarget(target)
    while Flinging and task.wait(0.015) do
        if target.Character and LP.Character then
            local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local myHRP = LP.Character:FindFirstChild("HumanoidRootPart")
            
            if tHRP and myHRP then
                -- Prediction pattern (8-direction cycle)
                local pattern = {
                    Vector3.new(7, 4, 7),
                    Vector3.new(-7, 4, -7),
                    Vector3.new(7, 4, -7),
                    Vector3.new(-7, 4, 7),
                    Vector3.new(10, 6, 0),
                    Vector3.new(-10, 6, 0),
                    Vector3.new(0, 6, 10),
                    Vector3.new(0, 6, -10)
                }

                -- Force network control
                if tHRP:GetNetworkOwner() ~= LP then
                    tHRP:SetNetworkOwner(LP)
                end

                -- Chaos fling cycle
                for _, offset in ipairs(pattern) do
                    if not Flinging then break end
                    
                    -- Position and velocity
                    myHRP.CFrame = tHRP.CFrame + offset
                    myHRP.Velocity = (tHRP.Position - myHRP.Position).Unit * 9000 
                                  + Vector3.new(0, 6000, 0)
                    
                    -- Rotational chaos
                    myHRP.RotVelocity = Vector3.new(
                        math.random(-100,100),
                        math.random(-100,100),
                        math.random(-100,100)
                    )
                    
                    -- Collision bypass
                    myHRP.CanCollide = false
                    tHRP.CanCollide = false
                    
                    task.wait(0.015)
                end
            end
        end
    end
end
