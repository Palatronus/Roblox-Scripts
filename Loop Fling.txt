local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local TweenService = game:GetService("TweenService")

-- Destroy existing GUI if present
if game:GetService("CoreGui"):FindFirstChild("FlingPanel") then
    game:GetService("CoreGui").FlingPanel:Destroy()
end

-- GUI Construction with forced visibility
local FlingGUI = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local InputBox = Instance.new("TextBox")
local ToggleButton = Instance.new("TextButton")
local StatusLabel = Instance.new("TextLabel")

FlingGUI.Name = "FlingPanel"
FlingGUI.ResetOnSpawn = false
FlingGUI.DisplayOrder = 999
FlingGUI.Parent = game:GetService("CoreGui")

MainFrame.Name = "Main"
MainFrame.Size = UDim2.new(0, 300, 0, 160)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -80)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = FlingGUI

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = MainFrame

InputBox.Name = "TargetInput"
InputBox.Size = UDim2.new(0.8, 0, 0.25, 0)
InputBox.Position = UDim2.new(0.1, 0, 0.1, 0)
InputBox.PlaceholderText = "Enter usernames (comma separated)"
InputBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
InputBox.TextColor3 = Color3.fromRGB(255, 255, 255)
InputBox.ClearTextOnFocus = false
InputBox.Parent = MainFrame

ToggleButton.Name = "FlingToggle"
ToggleButton.Size = UDim2.new(0.6, 0, 0.35, 0)
ToggleButton.Position = UDim2.new(0.2, 0, 0.4, 0)
ToggleButton.Text = "START FLING"
ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ToggleButton.TextColor3 = Color3.fromRGB(255, 50, 50)
ToggleButton.Parent = MainFrame

StatusLabel.Name = "Status"
StatusLabel.Size = UDim2.new(0.8, 0, 0.2, 0)
StatusLabel.Position = UDim2.new(0.1, 0, 0.8, 0)
StatusLabel.Text = "Status: Inactive"
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Parent = MainFrame

-- Add visual effects
TweenService:Create(
    MainFrame,
    TweenInfo.new(0.5, Enum.EasingStyle.Quint),
    {BackgroundTransparency = 0}
):Play()

-- Ensure UI elements have visible borders
for _, obj in pairs(MainFrame:GetChildren()) do
    if obj:IsA("GuiObject") then
        Instance.new("UIStroke", obj).Color = Color3.fromRGB(80, 80, 80)
    end
end

-- Rest of the fling functionality remains similar to previous version 
-- (from NuclearOrbit function downward) but with these UI fixes
