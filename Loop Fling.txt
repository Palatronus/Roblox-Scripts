local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local UIS = game:GetService("UserInputService")

-- Clear existing GUI
if game.CoreGui:FindFirstChild("FlingGUI") then
    game.CoreGui.FlingGUI:Destroy()
end

-- GUI Setup (Force Visibility)
local GUI = Instance.new("ScreenGui")
GUI.Name = "FlingGUI"
GUI.ResetOnSpawn = false
GUI.ZIndexBehavior = Enum.ZIndexBehavior.Global
GUI.DisplayOrder = 999
GUI.Parent = game.CoreGui

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 300, 0, 200)
Main.Position = UDim2.new(0.5, -150, 0.5, -100)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.Active = true
Main.Parent = GUI

-- Force GUI visibility
GUI.Enabled = true
task.wait()
GUI.Enabled = true

-- Rest of your code (username input, buttons, fling system, etc.)
-- [PASTE YOUR EXISTING CODE HERE STARTING FROM USERNAME INPUT] --
