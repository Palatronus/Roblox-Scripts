-- MODIFIED SECTION ONLY - Keep everything else exactly as you provided
local Flinging = false
local function FlingTarget(target)
    while Flinging and task.wait(0.005) do -- Ultra-fast updates
        if target.Character and LP.Character then
            local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local myHRP = LP.Character:FindFirstChild("HumanoidRootPart")
            
            if tHRP and myHRP then
                -- Chaotic spiral pattern
                local angle = tick() * 30 -- Hyper-speed rotation
                local spiralOffset = Vector3.new(
                    math.cos(angle) * 6, -- X spiral
                    math.sin(angle * 2) * 4 + 5, -- Y wave pattern
                    math.sin(angle) * 6  -- Z spiral
                )

                -- Nuclear velocity forces
                myHRP.CFrame = tHRP.CFrame + spiralOffset
                myHRP.Velocity = (tHRP.Position - myHRP.Position).Unit * 20000 
                                + Vector3.new(0, 10000, 0)
                
                -- Quantum spin forces
                myHRP.RotVelocity = Vector3.new(
                    math.random(-1000,1000),
                    math.random(-1000,1000),
                    math.random(-1000,1000)
                )

                -- Target annihilation
                tHRP.Velocity = myHRP.Velocity * 1.5
                tHRP.RotVelocity = myHRP.RotVelocity * 2
                
                -- Collision obliteration
                myHRP.CanCollide = false
                tHRP.CanCollide = false
            end
        end
    end
end
