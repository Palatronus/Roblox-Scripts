-- Enhanced Ultra-Fling System (Add/Replace the existing FlingTarget function)
local Flinging = false
local function FlingTarget(target)
    while Flinging and task.wait(0.01) do  -- Faster update rate
        if target.Character and LP.Character then
            local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local myHRP = LP.Character:FindFirstChild("HumanoidRootPart")
            
            if tHRP and myHRP then
                -- Chaotic Orbital Pattern
                local angle = tick() * 20  -- Time-based rotation
                local orbitOffset = Vector3.new(
                    math.cos(angle) * 4,   -- X axis orbit
                    math.random(1, 3),     -- Vertical offset
                    math.sin(angle) * 4    -- Z axis orbit
                )

                -- Aggressive Position/Velocity Manipulation
                myHRP.CFrame = tHRP.CFrame + orbitOffset
                myHRP.Velocity = (tHRP.Position - myHRP.Position).Unit * 15000 
                                + Vector3.new(0, 7500, 0)
                
                -- Target Disruption System
                myHRP.RotVelocity = Vector3.new(
                    math.random(-100,100),
                    math.random(-100,100),
                    math.random(-100,100)
                )

                -- Collision Bypass
                myHRP.CanCollide = false
                tHRP.CanCollide = false
            end
        end
    end
end
